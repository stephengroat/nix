sudo: required
services:
  - docker
install: docker build -t nix-build .
script: docker run -v "$PWD":/usr/src/nix nix-build "echo $SHELL && env && ./bootstrap.sh && ./configure --disable-doc-gen && make"
#os: osx
#script: ./tests/install-darwin.sh
